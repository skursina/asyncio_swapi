services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: swapi_user
      POSTGRES_PASSWORD: swapi_pass
      POSTGRES_DB: swapi
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: